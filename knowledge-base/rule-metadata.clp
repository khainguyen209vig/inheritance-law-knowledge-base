; Human-readable and legal metadata is data, not procedural rule logic.

(deffacts will-validity-rule-metadata
  (rule-metadata
    (rule-id R-B01)
    (module will-validity)
    (legal-source "Điều 630 khoản 1 điểm a Bộ luật Dân sự 2015")
    (description "Điều kiện về ý chí của người lập di chúc hợp lệ.")
    (status draft))
  (rule-metadata
    (rule-id R-B02)
    (module will-validity)
    (legal-source "Điều 630 khoản 1 điểm b Bộ luật Dân sự 2015")
    (description "Điều kiện về nội dung và hình thức của di chúc hợp lệ.")
    (status draft))
  (rule-metadata
    (rule-id R-B03)
    (module will-validity)
    (legal-source "Điều 630 Bộ luật Dân sự 2015")
    (description "Di chúc đáp ứng các điều kiện hợp pháp cơ bản.")
    (status draft))
  (rule-metadata
    (rule-id R-B04)
    (module will-validity)
    (legal-source "Điều 630 khoản 1 điểm a Bộ luật Dân sự 2015")
    (description "Di chúc không hợp pháp do ý chí của người lập di chúc không hợp lệ.")
    (status draft))
  (rule-metadata
    (rule-id SYSTEM-CONFLICT)
    (module will-validity)
    (legal-source "")
    (description "Có các kết luận trái ngược nhau trong cùng vụ việc.")
    (status draft))
  (rule-metadata
    (rule-id SYSTEM-INCOMPLETE)
    (module will-validity)
    (legal-source "")
    (description "Chưa đủ dữ kiện để đưa ra kết luận.")
    (status draft)))

